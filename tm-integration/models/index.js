'use strict';

const DailySale = require('./dailySale');
const Order = require('./order');
const Parameter = require('./parameter');

const models = { DailySale, Order, Parameter };

module.exports = models;